# =============================================================================
# Pipeline FPS Telemetry - variables compartidas y configuración comun
# =============================================================================

# --- Bases de datos (OBLIGATORIO) -------------------------------------------
# Usadas por ingest-api (Mongo) y metrics-worker (ambas)
MONGO_URL=mongodb+srv://marcpe03_db_user:wp9cZzTsW_NvFh_@uaj-02-mongodb.jsdk9di.mongodb.net/?appName=UAJ-02-MongoDB

# Usada por metrics-worker (escribe) y query-api (lee)
POSTGRES_URL=postgresql://postgres.xdweihmlovfmoqbpjnuq:zVoX0oJYJJ2K5EHP@aws-0-eu-west-1.pooler.supabase.com:5432/postgres


# =============================================================================
# Query API · todas opcionales (tienen defaults sensatos en config.py)
# Descomenta solo las que quieras sobrescribir.
# =============================================================================

# --- Pool de conexiones a Postgres ------------------------------------------
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_RECYCLE=1800

# --- Servidor ---------------------------------------------------------------
QUERY_API_HOST=0.0.0.0
QUERY_API_PORT=8001

# --- CORS -------------------------------------------------------------------
# Cuando montes el frontend, añade aquí su URL. Formato: lista JSON.
# Defaults ya incluyen localhost:3000 (CRA) y localhost:5173 (Vite).
# CORS_ORIGINS=["http://localhost:3000","http://localhost:5173","https://mi-frontend.com"]

# --- Logging ----------------------------------------------------------------
LOG_LEVEL=INFO